services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - feedback-data:/app/data
    environment:
      - ADMIN_USER=admin
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-changeme}
    restart: unless-stopped

volumes:
  feedback-data:
