services:
  web:
    build: .
    ports:
      - "8389:8389"
    volumes:
      - feedback-data:/app/data
    environment:
      - ADMIN_USER=admin
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-changeme}
    restart: unless-stopped

volumes:
  feedback-data:
